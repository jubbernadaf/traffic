services:
  - type: web
    name: traffic-2
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    runtime: python-3.11  # Alternative syntax
